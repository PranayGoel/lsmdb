// Intentionally empty translation unit so the lsmdb target has something to
// compile before wal.cpp/memtable.cpp/etc. land. Removed once the first real
// component (the WAL) is added -- see DESIGN.md's changelog.
